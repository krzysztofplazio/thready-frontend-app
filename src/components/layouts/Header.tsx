import { Box } from "@mui/material";
import './Header.scss';

export default function Header() {
    return(
        <header>
            <Box className='header-box'>
                <span>Dupa</span>
            </Box>
        </header>
    );
}